spring:
  application:
    name: ops-service
  datasource:
    url: jdbc:mysql://localhost:3306/gamehot?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: ${DB_USER:gamehot}
    password: ${DB_PASS:GameHot@2026!}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASS:HuHuDiDi@2026!}
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:ceasejoy}
    password: ${RABBITMQ_PASS:HuHuDiDi@2026!}

server:
  port: 8085

jwt:
  secret: ${JWT_SECRET:GameHot-JWT-Secret-Key-2026-MustBe32C}

feishu:
  app-id: ${FEISHU_APP_ID:}
  app-secret: ${FEISHU_APP_SECRET:}
  webhook-url: ${FEISHU_WEBHOOK_URL:}

logging:
  level:
    com.gamehot: INFO
